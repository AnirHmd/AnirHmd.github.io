const clock=document.getElementById('clock'),button=document.querySelector('.start-button'),menu=document.getElementById('start-menu');function tick(){clock.textContent=new Intl.DateTimeFormat('fr-CA',{hour:'2-digit',minute:'2-digit'}).format(new Date())}tick();setInterval(tick,30000);function closeMenu(){menu.hidden=true;button.setAttribute('aria-expanded','false')}button.addEventListener('click',()=>{const open=menu.hidden;menu.hidden=!open;button.setAttribute('aria-expanded',String(open))});document.addEventListener('click',e=>{if(!menu.hidden&&!menu.contains(e.target)&&!button.contains(e.target))closeMenu()});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&!menu.hidden){closeMenu();button.focus()}});

